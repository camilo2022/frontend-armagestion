import { ConfigurationCampaigns } from './configuration-campaigns';

describe('ConfigurationCampaigns', () => {
  it('should create an instance', () => {
    expect(new ConfigurationCampaigns()).toBeTruthy();
  });
});
